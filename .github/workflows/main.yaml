name: using composite
on: 
  push:
    branches:
      - "**"
jobs:
  greeting-job:
    runs-on: ubuntu-latest
    steps:
      - name:  "itd steps 2"
        uses: vamsins/.github/actions/greet@main
